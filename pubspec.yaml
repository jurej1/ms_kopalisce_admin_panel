name: ms_kopalisce_admin_panel
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  bloc: 7.0.0
  cloud_firestore: ^4.3.1
  coupon_repository:
    git:
      url: https://github.com/jurej1/coupon_repository.git
      ref: master
  cupertino_icons: ^1.0.2
  equatable: ^2.0.5
  firebase_auth: ^4.2.4
  firebase_core: ^2.4.1
  flutter:
    sdk: flutter
  flutter_bloc: 7.0.0
  formz: ^0.4.1
  qr_flutter: ^4.0.0
  price_repository:
    git:
      url: https://github.com/jurej1/price_repository.git
      ref: master
  quiz_repository:
    git:
      url: https://github.com/jurej1/quiz_repository.git
      ref: master


dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
